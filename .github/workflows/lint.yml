name: Lint

on:
  push:
    branches: [develop, main]
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  lint:
    runs-on: ubuntu-22.04
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.12"]
        poetry-version: ["2.1.0"]

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - uses: abatilo/actions-poetry@v3
        with:
          poetry-version: ${{ matrix.poetry-version }}

      - name: Add poetry to PATH
        run: echo "$HOME/.local/bin" >> $GITHUB_PATH

      - name: Use in-project virtualenv
        run: poetry config virtualenvs.in-project true

      - name: Restore venv cache
        uses: actions/cache@v4
        with:
          path: .venv
          key: venv-${{ runner.os }}-py${{ matrix.python-version }}-poetry${{ matrix.poetry-version }}-lint-${{ hashFiles('**/poetry.lock') }}

      - name: Install main + lint dependencies
        run: poetry install --with lint

      - name: ruff check
        run: poetry run ruff check protea scripts

      - name: mypy
        run: poetry run mypy protea

      - name: smell budget (master plan v3.2 §3)
        run: poetry run python scripts/check_smells.py

      - name: em-dash check (no em-dashes in publishable prose)
        run: python scripts/check_no_em_dashes.py

      - name: doc ref drift gate (DS.2)
        run: poetry run python scripts/check_doc_refs.py

      - name: autodoc coverage gate (DR.1)
        run: python scripts/check_autodoc_coverage.py
