PROTEA has been installed as release "{{ .Release.Name }}" in namespace "{{ .Release.Namespace }}".

Components deployed:
  postgres (pgvector): {{ if .Values.database.internal }}enabled{{ else }}external ({{ .Values.database.externalUrl | default "set via database.externalUrl" }}){{ end }}
  rabbitmq:            {{ if .Values.amqp.internal }}enabled{{ else }}external{{ end }}
  minio:               {{ if .Values.objectStore.enabled }}enabled{{ else }}disabled{{ end }}
  api:                 replicas={{ .Values.api.replicaCount }}
  frontend:            {{ if .Values.frontend.enabled }}enabled{{ else }}disabled{{ end }}

Workers enabled:
{{- range $key, $worker := .Values.workers }}
{{- if $worker.enabled }}
  - worker-{{ $key | kebabcase }} (queue={{ $worker.queue }}, replicas={{ $worker.replicaCount | default 1 }})
{{- end }}
{{- end }}

Reach the API in-cluster at:
  http://{{ include "protea.componentName" (dict "ctx" . "component" "api") }}:{{ .Values.api.service.port }}

To get the API service externally (when not using ingress):
  kubectl --namespace {{ .Release.Namespace }} port-forward svc/{{ include "protea.componentName" (dict "ctx" . "component" "api") }} 8000:{{ .Values.api.service.port }}

Tail the API logs:
  kubectl --namespace {{ .Release.Namespace }} logs -l app.kubernetes.io/component=api -f
