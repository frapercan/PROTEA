"""Shared test helpers.

This package centralises utilities that test modules import. Anything
here is non-test infrastructure (no `test_*` functions), so it must not
be discovered as a test module by pytest.
"""
